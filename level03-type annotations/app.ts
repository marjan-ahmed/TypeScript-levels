let myName : string = "marjan"; //It is name, so we declare it as a string  
console.log(myName);

let myAge : number = 15; //It is age, so we declare it as a number
console.log(myAge);

let eatFood : boolean = true; //It is a question, so we declare it as a boolean
console.log(eatFood);
