"use strict";
let myName = "marjan"; //It is name, so we declare it as a string  
console.log(myName);
let myAge = 15; //It is age, so we declare it as a number
console.log(myAge);
let eatFood = true; //It is a question, so we declare it as a boolean
console.log(eatFood);
